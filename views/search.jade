extends layout

block content
  h1 #{name} 

  if searchResults.length > 0
    ul.mt-4
      each result, i in searchResults 
        li.name=result.name 
        p.result-details=result.details
        p.result-details=result.price    
  else 
    p.not Poojas Not Found   